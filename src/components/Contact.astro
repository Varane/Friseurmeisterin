---
import { getSiteData } from '@lib/site';
const site = getSiteData();
---
<section id="contact" class="container-pad py-16">
  <div class="grid gap-8 lg:grid-cols-[1.2fr_1fr]">
    <div class="space-y-4">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-ink/50">Kontakt</p>
      <h2 class="font-serif text-3xl font-semibold text-ink md:text-4xl">Termin &amp; Beratung</h2>
      <p class="text-sm text-ink/70">PLACEHOLDER_CONTACT_TEXT</p>
      <div class="grid gap-2 text-sm text-ink/70">
        <p>{site.contact.address}</p>
        <p>{site.contact.phone}</p>
        <p>{site.contact.email}</p>
      </div>
      <div class="flex flex-wrap gap-4">
        <a class="btn-primary" href={site.bookingUrl}>
          {site.heroCtaLabel}
        </a>
        <a class="btn-secondary" href={site.contact.mapsUrl}>
          Google Maps
        </a>
      </div>
    </div>
    <div class="card flex flex-col justify-between gap-6 p-6">
      <div>
        <p class="text-xs font-semibold uppercase tracking-[0.3em] text-ink/50">Hinweis</p>
        <p class="mt-3 text-sm text-ink/70">
          PLACEHOLDER_CONTACT_NOTE
        </p>
      </div>
      <div class="rounded-xl border border-line bg-mist p-4 text-xs text-ink/70">
        PLACEHOLDER_OPENING_HOURS
      </div>
    </div>
  </div>
</section>
